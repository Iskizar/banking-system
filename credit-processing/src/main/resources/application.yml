spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/credit_db
    username: postgres
    password: 123123
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/credit/db.changelog-master.yaml
server:
  port: 8082